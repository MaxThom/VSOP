
class Main {
    main(test: Men, test2: Human, test3: Boy) : int32 {

        test <- new Human; // WRONG
        test2 <- new Men; // RIGHT
        test3 <- new Men; // WRONG
        test3 <- new Human; // WRONG
        test <- new Boy; // RIGHT
        test2 <- new Men; // RIGHT
        test2 <- new Boy; // RIGHT
        test3 <- new Boy; // RIGHT
        test <- new Men; // RIGHT
        test2 <- new Human; // RIGHT
        test2 <- new Women; // WRONG

        let j : Men in new Men;
        let b : Men <- if (true) then new Men else new Men in new Men;
        let p : Men <- if (true) then new Boy else new Men in new Men;
        let d : Men <- if (true) then new Human else new Human in new Human;

        0
    }
}

class Boy extends Men {

    drinkWater(amout: int32) : bool {

        gotBigDick <- true;
        true
    }
}

class Men extends Human {

    gotBigDick: bool;

    drinkBeer(amout: int32) : bool {

        gotBigDick <- true;
        true
    }
}

class Human {
    age : int32;
    name : string;
    isSick : bool;

    eatFood(amout: int32) : bool {
        true
    }
}

