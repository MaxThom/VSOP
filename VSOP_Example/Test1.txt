class MyClass {
    myField : bool <- if (true) then "" else 32;
}
class Main {
        main() : int32 {
            if (abc = true) then {
                ""
            } else {
                true
            }

            while (true) do {

            }

            let test: int32 in {0};

            (*let xs : List <- (new Cons).init(0, (new Cons).init(
                                             1, (new Cons).init(
                                             2, new Nil)))
                in print("List has length ").printInt32(xs.length()).print("\n");*)



            878 + 232
        }
}