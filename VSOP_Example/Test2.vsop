class Main extends IO {
    io : IO;

    useIO(io : IO) : IO {
        io.print("This argument looks like it is a valid IO object.\n")
    }

    main() : int32 {
        io <- self; // OK, as Main extends IO
        useIO(io); // Should not trigger any error, as io is a valid IO
        0
    }
}