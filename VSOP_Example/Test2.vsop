class Parent {
    name() : int32 { 50 }
}
class Child extends Parent {
    name() : int32 { 20 }
}
class Main extends IO {
    main() : int32 {
        printInt32((if true then new Parent else new Child).name());
        print("\n");
        printInt32((if false then new Parent else new Child).name());
        print("\n");
        0
    }
}