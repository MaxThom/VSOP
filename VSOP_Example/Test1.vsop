class Main extends IO {
    main() : int32 {
        print("Enter a string:\n");
        let s : string <- inputLine() in print(s);
        print("Enter a number:\n");
        let i : string <- inputLine() in print(i);
        print("\nEnter a boolean:\n");
        let b : string <- inputLine() in print(b);
        print("\nEnter another boolean:\n");
        let b : string <- inputLine() in print(b);
        print("\n");
        0
    }
}
